export class Button {
    cx: number;
    cy: number;
    width: number;
    height: number;

    constructor(cx: number, cy: number, width: number, height: number) {
        this.cx = cx;
        this.cy = cy;
        this.width = width;
        this.height = height;
    }
    
    
}

